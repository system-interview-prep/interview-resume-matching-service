version: '3.8'

services:
  backend:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: resume-ranking-backend
    ports:
      - "5001:5001"
    env_file:
      - .env
    volumes:
      - .:/app
    restart: unless-stopped
